// import { expect } from '@playwright/test';
// //import { test } from '../../api/fixtures/apiFixture';
// import { test } from '../../api/fixtures/apiFixture';

// test.describe('Member Agreement Flow', () => {

//   test('Login and fetch member agreement successfully', async ({ memberService }) => {

//     const response = await memberService.getAgreement();

//     expect(response.status()).toBe(200);

//     const body = await response.json();
//     console.log(body.message);

//     expect(body.message).toBe('Successfully fetched the agreement URL.');
//   });

//   test('Member dashboard data fetched successfully', async ({ memberService }) => {

//     const response = await memberService.getDashboardDetails();

//     expect(response.status()).toBe(200);

//     const body = await response.json();
//     console.log(body.message);

//     expect(body.message).toBe('Successfully found the records to show on the Member dashboard.');
//   });


//   test('Create Member With Payment', async ({ memberService }) => {



// });

// });
